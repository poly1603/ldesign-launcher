# 使用 pnpm
package-manager=pnpm

# 自动安装 peer dependencies
auto-install-peers=true

# 使用严格的 peer dependencies
strict-peer-dependencies=false

# 保存精确版本
save-exact=false

# 引擎严格模式
engine-strict=true

# 公共仓库配置
registry=https://registry.npmjs.org/
