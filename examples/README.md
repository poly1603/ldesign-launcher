# @ldesign/launcher 示例项目

本目录包含了 `@ldesign/launcher` 支持的所有前端框架的演示示例。

## 框架支持

| 框架 | 目录 | 端口 | 描述 |
|------|------|------|------|
| Vue 3 | `vue3-demo` | 5173 | Vue 3 + Composition API |
| React | `react-demo` | 5174 | React 18 + Hooks |
| Svelte | `svelte-demo` | 5175 | Svelte 4 |
| Solid | `solid-demo` | 5176 | SolidJS |
| Preact | `preact-demo` | 5177 | Preact (3KB React Alternative) |
| Vanilla | `vanilla-demo` | 5178 | 纯 TypeScript |

## 快速开始

### 安装依赖

在项目根目录执行：

```bash
pnpm install
```

### 运行示例

进入任意示例目录并启动开发服务器：

```bash
cd vue3-demo
pnpm dev
```

或者使用 launcher CLI：

```bash
launcher dev
```

### 构建生产版本

```bash
pnpm build
```

### 预览构建产物

```bash
pnpm preview
```

## 功能演示

每个示例都展示了以下功能：

- ⚡️ **快速的 HMR** - 热模块替换
- 🎨 **框架特性** - 各框架的核心特性
- 📦 **零配置** - 无需额外配置即可运行
- 🔧 **TypeScript 支持** - 完整的类型支持

## 快捷键

在开发服务器运行时，可以使用以下快捷键：

- `h` - 显示帮助
- `c` - 清屏
- `o` - 在浏览器中打开
- `r` - 重启服务器
- `u` - 显示服务器地址
- `q` - 退出

## 诊断工具

如果遇到问题，可以运行诊断命令：

```bash
launcher doctor
```

## 更多信息

请参阅 [@ldesign/launcher 文档](../README.md)。
